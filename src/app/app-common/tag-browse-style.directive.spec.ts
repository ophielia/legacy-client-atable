import {TagBrowseStyleDirective} from './tag-browse-style.directive';

describe('TagBrowseStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TagBrowseStyleDirective();
    expect(directive).toBeTruthy();
  });
});
